// Every language the build ships. Adding one: copy the `en/` folder, translate,
// import it here — that is the whole registration (README.md next to this file).

import de from './de/index.js';
import en from './en/index.js';

export const LOCALES = { de, en };
